# Dependencies
node_modules/

# Build outputs
dist/
.output/
.nitro/

# Generated (overwritten on every build). These embed absolute local paths,
# so the whole stubs directory is ignored, not just the page manifest.
server/stubs/
routes.generated.ts

# Playwright
test-results/
playwright-report/

# TypeScript build info
*.tsbuildinfo

# OS / editor
.DS_Store
.env
.env.*
*.local
