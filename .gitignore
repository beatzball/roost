.DS_Store
*.log

# Claude Code local settings — machine-specific, may contain local paths.
# Only a *global* gitignore protects this on the author's machine; contributors
# cloning this repo have no such protection, so it is ignored here too.
.claude/settings.local.json

# SDD scratch (progress ledger, task briefs)
.superpowers/
